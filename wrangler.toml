# Cloudflare Pages: static build + Functions in /functions
# Build: npm run build  →  output: dist/
# Dev:   npm run pages:dev  (needs .dev.vars with RESEND_API_KEY for subscribe)
# Deploy: npm run pages:deploy  (set secrets in the Pages project dashboard or CLI)

name = "the-last-frame"
compatibility_date = "2024-11-12"
pages_build_output_dir = "dist"
